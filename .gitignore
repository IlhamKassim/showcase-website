# Ignore Mac system files
.DS_Store

# Ignore Python cache if we run the scraper locally
__pycache__/
*.pyc

# Ignore the live data file so it doesn't spam your commit history
solar_data.json.claude/
